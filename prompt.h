#pragma once
#include <string>
#include <vector>
#include <iostream>
using namespace std;

size_t promptForOptions(const string &prompt, const vector<string> &options);
int getRandom(int a, int b);
